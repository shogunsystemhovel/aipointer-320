# AIPointer reads its OpenRouter API key from the encrypted local settings store,
# not from environment variables. This file is here only as a reminder that no
# .env file is required to run the app.
#
# Get an OpenRouter key at https://openrouter.ai/keys
# Paste it into the Settings modal in the app (Cmd+, on macOS).
